{% extends '@User/userLayout.html.twig' %}

{% block title %}
    Se connecter
{% endblock %}

{% block content %}
    <div class="bgimg-1 w3-display-container w3-opacity-min" id="home">
        <div class="w3-display-middle w3-black w3-padding-large" style="white-space:nowrap;max-width: 330px ">
            <div class="w3-center w3-padding-large w3-animate-opacity">
                <h1 class="w3-xxxlarge">Connexion</h1>
            </div>

            <div class="flashbag-wrapper">
                {% for flashSuccess in app.session.flashbag.get('success') %}
                    <p class="w3-panel w3-green w3-padding">{{ flashSuccess }}</p>
                {% endfor %}
                {% for flashError in app.session.flashbag.get('errors') %}
                    <p class="w3-panel w3-red w3-padding">{{ flashError }}</p>
                {% endfor %}
            </div>

            <div class="w3-padding-large w3-xlarge">
                {{ form_start(form, { 'attr': {'class': 'w3-container'} }) }}
                {{ form_widget(form) }}
                <div class="w3-row w3-padding-16">
                    <button type="submit" class="w3-btn w3-small w3-left w3-green w3-col s5">Connexion</button>
                    <a href="{{ path('user_homepage') }}" type="submit" class="w3-btn w3-small w3-right w3-red w3-col s5">Annuler</a>
                </div>
                <a href="{{ path('authentication_create') }}" class="w3-col s5 w3-text-blue w3-medium">Créer un compte</a>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}